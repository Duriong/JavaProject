package ch02;

public class VarInitTest {
	//ma + Ctrl + Space => main 함수 자동 생성
	public static void main(String[] args) {
		int index = 2; //지역변수 선언
		//지역변수를 활용할때는 반드시 초기화가 되어야한다.
		
		index = index + 1;
		System.out.println("index : " + index);

	}

}
